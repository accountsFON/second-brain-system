---
name: vault-bridge
description: Shared second brain bridge for Codex. Use when a task needs vault context or a workflow listed in skills/README.md.
---

# Vault Bridge

Read `CLAUDE.md`, `context/learned-rules.md`, and the latest daily log before vault work.

When the user names a vault workflow, read the matching `skills/<name>.md` file completely and execute it. The `skills/` folder is canonical. Do not copy full workflow instructions into this bridge.
